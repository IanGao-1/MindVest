USE financial_management;

-- Sample data is now seeded at application startup from the cached price API.
-- Supported starter tickers are initialized in code when there are no transactions:
-- AAPL, TSLA
--
-- You can then add the remaining supported tickers manually through transactions:
-- C, AMZN, FB
